import React ,{useState} from 'react';
import Movie from './Movie';


export default function Movielist() {

    return (
        <>
            {/* {movies.map(val => (
                <Movie name={val.name} key={val.id} price={val.price}></Movie>
            ))} */}
        </>
    )
}